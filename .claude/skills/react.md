# Skill: React

**Used** in the web app. React 19 + TypeScript + Tailwind.
- Small, typed components; `"use client"` only when interactivity/state is needed.
- Co-locate state; keep fetch logic in clear handlers. See `.claude/templates/component.md`, `hook.md`.
