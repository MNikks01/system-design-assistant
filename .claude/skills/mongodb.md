# Skill: MongoDB

**Not used.** Persistence is **PostgreSQL + pgvector** (DECISION_LOG D-004). The MVP engines use an in-memory store; production swaps to Postgres (relational + vector search in one store). Do not introduce MongoDB without a superseding decision.
