# Skill: Firebase

**Not used.** Auth/DB/hosting are not on Firebase. Plan: **Clerk** (auth), **Postgres + pgvector** (data), **Vercel** (web hosting), **Stripe** (billing). If a Firebase capability is ever needed, record the rationale in `.claude/memory/decisions.md` first.
