# Roadmap — System Design Assistant

- **MVP (done):** the engine + engine/ (engine), mcp-server/ (MCP server), web/ (Next.js app), tested headlessly (unit/integration/typecheck/contract/e2e).
- **Next (V1):** wire real services behind the existing interfaces — LLM/embeddings keys, Postgres + pgvector, auth + billing, deploy. Browser UI verification.
- **Later (V2+):** see the root `ROADMAP.md` / capability ladder in `README.md`.

> Status is honest: dev/test paths are proven; production needs keys + infra.
