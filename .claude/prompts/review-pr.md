# Prompt: Review a PR

Review for: correctness + edge cases; tests added and passing; no secrets; inputs validated; errors handled; engine changes re-synced (drift green); strict types; minimal, readable diff; Conventional Commit; PR template filled. Flag anything that overclaims built-vs-planned. Be specific and cite `file:line`.
