# Prompt: Write/refresh docs

For: $ARGUMENTS

- Keep accurate + code-derived: subproject READMEs, root `API.md`, `.claude/project/*`, `CHANGELOG.md`.
- State clearly what is **built vs planned**. Prefer examples that actually run.
