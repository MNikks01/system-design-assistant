{
  "name": "system-design-assistant-workspace",
  "private": true,
  "type": "module",
  "description": "Dev workspace for system-design-assistant: type-checking, tests, and git hooks. Subprojects (engine, mcp-server, web) have their own package.json.",
  "scripts": {
    "test": "npm run typecheck && npm run test:unit",
    "test:unit": "node --test engine/test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.6",
    "typescript": "^5.6.0",
    "@types/node": "^22.7.0"
  },
  "engines": {
    "node": ">=24"
  }
}